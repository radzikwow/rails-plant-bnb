
  <div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel">
    <div class="banner">
      <div class="banner-content">
        <div class="banner-text">
          <h1>PlantBnB</h1>
          <p>The best for your plant babies</p>
        </div>

        <div class="banner-buttons">
          <%= link_to new_offer_path(@offer) do %>
            <div class="btn btn-ghost">Offer plantkeeping
            </div>
          <% end %>
          <%= link_to offers_path(@offer) do %>
            <div class="btn btn-ghost" data-action="click->home#hideCards">Look for a plantkeeper
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="carousel-inner">
      <div class="carousel-item carousel-div  one active">
      </div>
      <div class="carousel-item carousel-div two">
      </div>
      <div class="carousel-item carousel-div three">
      </div>
    </div>
  </div>


<!-- <div class="banner-text">
    <h1>PlantBnB</h1>
    <p>The best for your plant babies</p>
  </div>
  <div class="banner-buttons">
    <%# <%= link_to offers_path(@offer) do%>
      <div class="btn btn-ghost" data-action="click->home#hideCards">Look for a plantkeeper
      </div>

    <%# <% end % %>
    <%= link_to new_offer_path(@offer) do %>
      <div class="btn btn-ghost">Offer plantkeeping
      </div>
    <% end %>
  </div> -->
